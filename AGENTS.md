# Mobile Wedding Invitation Agent System (AGENTS.md)

이 문서는 모바일 청첩장 프로젝트의 단일 통합(Single Unified) 에이전트 개발 및 검증 하네스 체계 가이드라인입니다. 모든 청첩장 기능과 디자인은 루트 디렉토리의 단일 `index.html` 파일로 통합 관리됩니다.

---

## 1. 시스템 개요 및 아키텍처 원칙

- **단일 통합 파일 원칙 (Single Source of Truth)**:
  - 분리된 하위 폴더(`standard-invitation`, `friends-invitation` 등)는 사용하지 않으며, 루트의 `index.html` 하나에서 모든 모바일 청첩장 기능(인스타그램 UI, 스토리 뷰어, BGM, 지도/네비게이션 연동, 참석 투표, 간편송금, 방명록)을 단일 통합하여 제공합니다.
- **아키텍처 패턴**: **Single Unified Producer & Reviewer**
  - 모든 디자인, 컴포넌트, 인터랙션 수정은 오직 `index.html`에 직접 반영합니다.
  - 무이모지(Zero Emoji - SVG 아이콘 전용), 모바일 반응형(375~480px) 뷰포트, JS 런타임 무결성을 검증합니다.

```
┌─────────────────────────────────────────────────────────────┐
│                       index.html                            │
│           (단일 통합 모바일 청첩장 메인 엔트리포인트)          │
└──────────────────────────────┬──────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────┐
│                 Verification & Quality Engine               │
│   (무이모지 SVG 검증 / 모바일 반응형 / 딥링크 방어코드 검증)   │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. 핵심 규격 및 개발 표준

1. **단일 HTML 완결성 (Self-contained)**:
   - HTML, CSS, JavaScript를 루트 `index.html`에 일원화하여 배포 및 유지보수가 즉각적이도록 구성합니다.
2. **에셋 및 리소스 경로**:
   - 이미지: `images/` (배경, 프로필, 갤러리, 아이콘 등)
   - 음원: `music/` (`music/the_mountain-wedding.mp3` 등)
3. **무이모지 정책 (Zero Unicode Emoji)**:
   - 렌더링 파편화를 유발하는 유니코드 이모지 대신 인라인 SVG 또는 WebP/SVG 브랜드 아이콘을 100% 사용합니다.
4. **미설치 대비 방어 코드 (Fail-safe Deep Linking)**:
   - 네비게이션(네이버 지도/티맵/카카오내비) 및 간편송금(카카오페이/토스/카카오뱅크)은 모바일 환경별 Intent 및 Visibility Guard를 적용하여 앱 미설치 시에도 안전하게 웹/스토어로 전환되도록 유지합니다.

---

## 3. 구성 변경 이력 (Change History)

| 변경 일자 | 구성 유형 | 주요 변경 사항 |
| :--- | :--- | :--- |
| **2026-08-31** | **단일 통합 (Unified)** | • `standard-invitation`, `friends-invitation` 분리 폴더 완전 제거<br>• 루트 `index.html` 단일 통합 구조로 하네스 가이드라인 전면 일원화<br>• BGM 오토플레이, 3대 네비게이션/3대 간편송금 방어 엔진 및 방명록 이름 입력 기능 단일 통합 완료 |
